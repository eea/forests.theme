<tal:block tal:define="foo python:request.set('disable_plone.leftcolumn', 1);
        bar python:request.set('disable_plone.rightcolumn', 1);"/>
<html xmlns="http://www.w3.org/1999/xhtml"
      xmlns:metal="http://xml.zope.org/namespaces/metal"
      xmlns:tal="http://xml.zope.org/namespaces/tal"
      xmlns:i18n="http://xml.zope.org/namespaces/i18n"
         i18n:domain="plone.z3cform"
         metal:use-macro="context/main_template/macros/master">

<body>
<metal:css fill-slot="style_slot">
    <link type="text/css" rel="stylesheet" href="/++resource++select2/select2.css" />
    <link type="text/css" rel="stylesheet" href="//cdn.datatables.net/1.10.19/css/jquery.dataTables.min.css" />
    <link type="text/css" rel="stylesheet" href="//cdn.datatables.net/fixedcolumns/3.2.6/css/fixedColumns.dataTables.css" />
    <link type="text/css" rel="stylesheet" href="//malihu.github.io/custom-scrollbar/jquery.mCustomScrollbar.min.css" />
    <style>

        .matrix-content h3 {
            color: #737373;
            font-weight: bold;
        }

        .matrix-content h4 {
            font-weight: bold;
        }

        #matrix_main_table {
            border-top: 5px solid #0d8442;
            border-bottom: 5px solid #0d8442;
        }

        #matrix_main_table th {
            border-bottom: 2px solid #0d8442;
        }

        #matrix_main_table thead tr th:first-child,
        #matrix_main_table tbody tr td:first-child {
            border-right: 2px solid #0d8442;
        }

        #matrix_main_table thead tr th:last-child,
        #matrix_main_table tbody tr:last-child,
        #matrix_main_table tbody tr:nth-last-child(2),
        #matrix_main_table tbody tr td:last-child {
            color: #0d8442;
            font-weight: bold;
        }

        #matrix_main_table tbody tr:nth-last-child(2) {
            border-top: 2px solid #000;
        }

        #matrix_main_table thead tr th:nth-child(odd) {
            color: #666;
        }

        #matrix_main_table thead tr th:last-child,
        #matrix_main_table tbody tr td:last-child {
            border-left: 2px solid #000;
        }

        #matrix_main_table tbody tr:not(:nth-last-of-type(-n+2)):hover {
            background-color: #f3f9eb;
        }

        #matrix_main_table tbody tr td {
            vertical-align: middle;
            text-align: center;
        }

        #matrix_main_table tbody tr:not(:nth-last-of-type(-n+2)) td:not(:first-child):hover {
           background-color: #8ec63f;
           color: #fff;
           cursor: pointer;
        }

        #matrix_main_table th:not(:first-child):not(:last-child) {
            font-weight: normal;
        }

        #matrix_main_table tbody tr td:first-child {
            text-align: left;
            min-width: 200px;
        }

        #matrix_main_table thead tr th {
            vertical-align: middle;
            padding: 8px 0;
        }

       #matrix_main_table thead tr th:first-child {
           padding-left: 8px;
       }

        #matrix_main_table thead tr th:nth-child(even),
        #matrix_main_table tbody tr td:nth-child(even),
        #matrix_table_wrapper table.dataTable th:nth-child(even),
        #matrix_table_wrapper table.dataTable td:nth-child(even) {
            background-color: #eee;
        }

        #matrix_main_table tbody tr:nth-child(even):not(:last-child),
        #matrix_table_wrapper table.dataTable tbody tr:nth-child(even) {
            color: #666;
        }

        #matrix_main_table thead tr th:not(:first-of-type) span {
            transform: rotate(270deg);
            line-height: 1.3;
            display: block;
            padding: 2em 0;
            text-align: center;
        }

        #matrix_table_wrapper #matrix_table {
            border-bottom: 5px solid #0d8442;
        }

        #matrix_table th:nth-of-type(4),
        #matrix_table td:nth-of-type(4),
        #matrix_table_wrapper table.dataTable th:nth-of-type(4),
        #matrix_table_wrapper table.dataTable td:nth-of-type(4) {
            display: none;
        }

        #matrix_table_wrapper table.dataTable th,
        #matrix_table_wrapper table.dataTable td {
            padding: 10px 15px;
            vertical-align: middle;
        }

        #matrix_table th {
            min-width: 95px;
            text-transform: uppercase;
            color: #7e7e7e;
            border-bottom: 2px solid #0d8442;
        }

        #matrix_table_wrapper table.dataTable thead th {
            text-transform: uppercase;
            color: #7e7e7e;
            border-bottom: 2px solid #0d8442;
        }

        .DTFC_Cloned th:nth-of-type(3),
        .DTFC_Cloned tr td:nth-of-type(3) {
            border-right: 2px solid #0d8442;
        }

        #matrix_table tbody tr td {
            vertical-align: middle;
        }

        #matrix_table thead tr th:nth-child(even),
        #matrix_table tbody tr td:nth-child(even) {
            background-color: #eee;
        }

        #matrix_filters {
            background-color: #0d8442;
            color: #fff;
            padding: 0.7em;
            display: -webkit-flex;
            display: -ms-flexbox;
            display: -moz-box;
            display: flex;
            flex-wrap: wrap;
            justify-content: flex-end;
        }

        #matrix_filters h4 {
            display: inline-block;
            text-transform: uppercase;
            font-weight:bold;
            margin: 1.5em 0 0 0;
        }

        #matrix_filters .select2-container {
            margin: 0;
        }

        #matrix_filters label {
            display: block;
            margin-left: 0.3em;
        }

        #matrix_filters .select2-choices {
            border-radius: 5px;
            padding-right: 30px !important;
        }

        #matrix_filters  .select2-choices:after {
            content: "";
            position: absolute;
            right: 10px;
            top: 50%;
            transform: translateY(-50%);
            border-top: 5px solid #333;
            border-left: 5px solid transparent;
            border-right: 5px solid transparent;
        }


        #matrix_filters .filter-wrapper {
            margin: 0 0.5em;
        }

        #matrix_filters .select2-search-choice {
            background-color: #41b783;
            background-image: none;
            border: none;
            color: #fff;
            padding: 5px 30px 5px 5px;
        }

        #matrix_filters .select2-container-multi .select2-search-choice-close {
            left: auto;
            color: #000;
            background-position: right -11px;
        }

        .select2-drop-active {
            border: 1px solid #aaa;
        }

        .select2-results {
            min-height: 300px;
        }

        .select2-results .select2-result-label {
            padding: 10px 7px;
        }

        .select2-results .select2-highlighted {
            background: #41b783;
        }

        .js-filters {
            max-width: 300px;
            min-width: 250px;
        }

        .main_table_link {
            display: none;
            color: #333;
            margin: 1em 0;
        }

        .main_table_link:hover {
            text-decoration: none;
            color: #0d8442;
        }

        /* pagination */

        .dataTables_wrapper .dataTables_paginate .paginate_button.current,
        .dataTables_wrapper .dataTables_paginate .paginate_button.current:hover {
            background-color: #fff;
            background: #0d8442;
            border: none;
            color: #fff !important;
        }

        .dataTables_wrapper .dataTables_paginate .paginate_button:hover {
            background: #8dc63f;
            border: none;
            color: #fff !important;
        }

        .table-top-scroll-wrapper {
            width: 100%;
            overflow-y: hidden;
            display: none;
        }

        .table-top-scroll {
            width: 4938px;
            overflow: auto;
            height: 1px;
        }

        .dataTables_wrapper .dataTables_info,
        .dataTables_wrapper .dataTables_paginate {
            margin-top: 1em;
        }

        /* custom scrollbar  */
        .mCS-inset-2-dark.mCSB_scrollTools .mCSB_draggerRail {
            border-color: #000;
            background-color: #fff;
        }

        .mCS-inset-2-dark.mCSB_scrollTools .mCSB_dragger .mCSB_dragger_bar {
            background-color: #3e3e3e;
            border-radius: 0;
            height: 12px;
            margin: 0;
            top: 2px;
            border-radius: 16px;
            position: relative;
        }

        .mCS-inset-2-dark.mCSB_scrollTools .mCSB_dragger .mCSB_dragger_bar:before {
            content: '• • •';
            position: absolute;
            color: #fff;
            margin-left: auto;
            margin-right: auto;
            left: 0;
            right: 0;
            top: -3px;
        }

        .mCSB_scrollTools.mCSB_scrollTools_horizontal {
            background-color: #8ec63f;
            opacity: 1;
            height: 25px;
        }

        .mCSB_horizontal.mCSB_inside>.mCSB_container {
            margin-bottom: 24px;
        }

        .mCS-inset-2-dark.mCSB_scrollTools .mCSB_buttonLeft,
        .mCS-inset-2-dark.mCSB_scrollTools .mCSB_buttonRight {
            height: 15px;
            margin-top: 5px;
        }

        .mCSB_scrollTools.mCSB_scrollTools_horizontal a+.mCSB_draggerContainer {
            margin: 5px 20px;
        }

        .mCS-inset-2-dark.mCSB_scrollTools_horizontal .mCSB_draggerRail {
            height: 12px;
        }

    </style>
</metal:css>

<metal:js fill-slot="javascript_head_slot">
    <script type="text/javascript" src="//cdn.datatables.net/1.10.19/js/jquery.dataTables.min.js"></script>
    <script type="text/javascript" src="//cdn.datatables.net/fixedcolumns/3.2.6/js/dataTables.fixedColumns.min.js"></script>
    <script type="text/javascript" src="//malihu.github.io/custom-scrollbar/jquery.mCustomScrollbar.concat.min.js"></script>
    <script type="text/javascript" src="/++resource++select2/select2.min.js"></script>
    <script>
        $(document).ready( function () {
            var $matrix_main_table = $('#matrix_main_table');
            var $matrix_filters = $("#matrix_filters");
            var $select_filters = $(".js-filters");
            var $from_select = $select_filters.eq(1);
            var $to_select = $select_filters.eq(2);
            var $matrix_table = $("#matrix_table");
            var active_datatable;

            $select_filters.select2({
                placeholder: 'Select an option'
            });

            $select_filters.on('change', function (e, data) {
                // avoid change logic when we clear data when clicking the original table link
                if (data) {
                    return;
                }

                // get all values from given selectors
                var $selects = $("select");
                var jsonFilters = {};
                $selects.each(function(idx, el) {
                    var values = $(el).val();
                    if (values) {
                        jsonFilters[el.name] = values;
                    }
                });

                // ajax query for new data will just populate the datatable rows
                $.getJSON("queryMatrixData", jsonFilters).done(function(data) {
                    var values = data['data'] || [];
                    active_datatable.clear().rows.add(values).draw();
                });

            });

            $matrix_main_table.find('td').click(function(ev) {
              var td = ev.target;
              var $row = $(ev.target).closest('tr');
              var $tds = $row.find('td');
              var num;

              $tds.each(function(idx, el) {
                if (el === td) {
                    num = idx;
                    return true;
                }
              });

              // find out which row and column value we've clicked on
              var thead = $matrix_main_table.find('th').eq(num);
              var from = $tds.eq(0).text();
              var to = thead.text().trim();
              if (["Final area", "Net change"].indexOf(from) !== -1 || td.innerText === from) {
                  return;
              }


              // change value of hidden selectors to the value we just clicked
              $from_select.val(from).trigger('change', {table_search: true});
              $to_select.val(to).trigger('change', {table_search: true});

              $.getJSON("queryMatrixData", {
                  From: [from],
                  To: [to]
              }).done(function(data) {
                  var headers, columns;
                  $matrix_main_table.hide();
                  $matrix_filters.removeClass('hidden');

                  if (!$matrix_table.hasClass('dataTable')) {
                    // initial datatable creation with columns and rows
                    headers = data['columnDefs'];
                    columns = [];

                    for(var header in headers) {
                        columns.push({ title: headers[header]});
                    }

                    active_datatable = $matrix_table.DataTable({
                        'columns': columns,
                        data: data['data'],
                        "lengthChange": false,
                        "searching": false,
                        "pageLength": 22,
                        fixedColumns: {
                            heightMatch: 'auto',
                            leftColumns: 3
                        },
                        scrollX: "100%",
                        scrollXInner: "150%",
                        "fnInitComplete": function() {
                            $('.table-top-scroll-wrapper').mCustomScrollbar({
                                theme: "inset-2-dark",
                                horizontalScroll: true,
                                axis: "x",
                                setLeft: "-1px",
                                scrollButtons: {
                                    enable: true,
                                    scrollType: "stepped"
                                },
                                callbacks: {
                                    whileScrolling:function() {
                                        var posLeft = $('#mCSB_1_container').css('left');
                                        $('#mCSB_2_container').css("left", posLeft);
                                        var valueLeft = $('#mCSB_1_dragger_horizontal').css('left');
                                        $('#mCSB_2_dragger_horizontal').css("left", valueLeft);
                                    }
                              }
                          });

                          $('.dataTables_scrollBody').mCustomScrollbar({
                              theme: "inset-2-dark",
                              horizontalScroll: true,
                              axis: "x",
                              setLeft: "-1px",
                              scrollButtons: {
                                  enable: true,
                                  scrollType: "stepped"
                              },
                              callbacks: {
                                  whileScrolling:function() {
                                      var posLeft = $('#mCSB_2_container').css('left');
                                      $('#mCSB_1_container').css("left", posLeft);
                                      var valueLeft = $('#mCSB_2_dragger_horizontal').css('left');
                                      $('#mCSB_1_dragger_horizontal').css("left", valueLeft);
                                  }
                            }
                        });

                        $(".dataTables_scrollHead").prependTo("#mCSB_2_container");
                      },
                    });
                  }
                  else {
                      // replace just the rows in case we already created the datatable
                      active_datatable.clear().rows.add(data['data']).draw();
                  }

                  $("#matrix_table_wrapper").show();

                  // recalculate the columns sizes when the table becomes visible
                  active_datatable.columns.adjust();

                  var tableWidth = $('#mCSB_2_container').css('width');
                  $('#mCSB_1_container').css('width', tableWidth);

                  $(".main_table_link, .table-top-scroll-wrapper").css('display', 'block');
              });

            });


            $(".main_table_link").click(function(ev) {
                // hide non filter table and empty filtered table
                ev.preventDefault();
                $("#matrix_table_wrapper, .table-top-scroll-wrapper").hide();
                $matrix_filters.addClass('hidden');
                $matrix_main_table.show();
                $select_filters.select2('data', null);
                $(this).hide();
            });
        });
    </script>
</metal:js>

<metal:content metal:fill-slot="content-title">
    <h1 class="documentFirstHeading">Land use reporting</h1>
</metal:content>
<metal:content metal:fill-slot="content">

    <div class="matrix-content clearfix"
        tal:define="tview nocall:context/queryMatrixData; data tview/get_table_data">
        <h3>TABLE 4.1 LAND TRANSITION MATRIX</h3>
        <h4>Areas and changes in areas between the previous and the current inventory year</h4>
        <a href="#" class="main_table_link">
            <i class="fa fa-reply"></i>
            Go back to the original table
        </a>
        <div id="matrix_filters" class="hidden" tal:define="filter_dict tview/get_table_filters; keys filter_dict/keys; values filter_dict/values">
            <h4>Filter by:</h4>
            <tal:rows tal:repeat="rows filter_dict">
                <div class="filter-wrapper">
                <label tal:attributes="for string:js-${rows}" tal:content="rows">From</label>
                <select class="js-from" tal:attributes="name string:${rows}; class string:js-${rows} js-filters" multiple="multiple">
                <tal:row tal:repeat="row python:values[repeat['rows'].index]">
                    <option tal:content="row" tal:attributes="value row">Alabama</option>
                </tal:row>
                </select>
                </div>
            </tal:rows>
        </div>
        <div class="table-top-scroll-wrapper">
            <div class="table-top-scroll"></div>
        </div>
        <table id="matrix_table"></table>
        <table class="table table-responsive" id="matrix_main_table">
                <thead>
                <tal:rows tal:repeat="rows data">
                    <tr tal:condition="python:repeat['rows'].index == 0">
                        <th tal:repeat="item rows">
                            <span tal:content="item">Heading name</span>
                        </th>
                    </tr>
                </tal:rows>
                </thead>

                <tbody>
                    <tal:rows tal:repeat="rows data">
                    <tr tal:condition="python:repeat['rows'].index != 0">
                        <td tal:repeat="item rows" tal:content="item">Column name</td>
                    </tr>
                    </tal:rows>
                </tbody>

        </table>
    </div>

</metal:content>

</body>
</html>
